import UIKit

/// Visual chrome aligned with `LaunchScreenBackground` in Assets and the embedded web shell.
enum WCSWebChrome {
    static let background = UIColor(red: 0.05, green: 0.12, blue: 0.23, alpha: 1)
    static let accent = UIColor(red: 0.05, green: 0.58, blue: 0.53, alpha: 1)
}
